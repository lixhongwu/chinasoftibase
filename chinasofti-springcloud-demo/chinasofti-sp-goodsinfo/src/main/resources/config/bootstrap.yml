
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: chinasofti-sp-config
  application:
    name: sp-goodsServices
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password456@localhost:8761/eureka
  instance:
    prefer-ip-address: true

